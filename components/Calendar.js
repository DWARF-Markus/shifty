import styled from 'styled-components';

const Calendar = () => {
  return (
    <CalendarWrapper>
      app
    </CalendarWrapper>
  );
}

const CalendarWrapper = styled.div`
  min-height: 100vh;
`;

export default Calendar;